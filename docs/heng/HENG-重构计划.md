# 「衡 / HENG」重构计划 —— 从 structdesign 演进为多国规范智能结构计算平台

> 依据 `衡-多国规范智能结构计算平台-产品设计书.md`。本文件是重构的活计划(survives compaction)。

## 现状 → 五层架构 的映射（findings）
| 产品层 | 设计书要求 | 现有 structdesign | 差距/动作 |
|---|---|---|---|
| L1 数据模型 SSM | 单一结构语义模型 + Git式版本化 + 审计日志 | `modeler/project.py`(Project/StandardFloor…) 是几何模型，非语义模型；无版本化 | 建 SSM seed：语义对象 + commit/branch/tag + 审计 |
| L2 计算内核 | FEM/非线性/时程/耦合/GPU | `structdesign/analysis/frame3d,modal3d,rspectrum3d,pdelta` 自研杆系FEM+反应谱+P-Δ | 保留；后续接非线性/水工 |
| **L3 规范引擎** | **Code-as-Data**：条文→规则记录(rule_id/scope/DSL formula/provenance/lineage)+受限DSL+每条附算例+CI+辖区解析+多国并行+NDP参数层 | `structdesign/codes/gb50010_*.py` 等**硬编码** checks | **核心重构**：把规范从代码剥离成数据。← 首先做 |
| L4 智能层 | 玻璃盒协议(可溯源/可编辑/可复核/可关闭)+NL建模+规范RAG问答+方案比选+优化+审图助手 | `modeler/commands.py`(NL命令+run_llm)+AI对话+trace.py(留痕) | 扩展：接规范引擎做条文级问答/校核；玻璃盒 |
| L5 交付层 | 计算书(多国样式)+图纸(平法/日式/欧式)+审查包+IFC | `dxf_export`(平法)+`calcbook_pro` | 保留；后续多国样式+审查包 |

## 重构原则
- **加法+不破坏**：现有 FEM/出图/GUI/测试(modeler 25+核心31)保持绿；新建 `heng/` 平台层，逐步把 codes 校核路由到规范引擎。
- **验证驱动(项目铁律=设计书§4.1 CI要求)**：每条规则(Rule)附**官方算例/手算基准**为测试用例，`test_heng_rules.py` 全跑。
- **确定性优先(设计书§4.3一级/§6.1)**：辖区解析、合规判定走确定性规则表，不用 AI；AI 只检索/起草/解释。
- **多国**：rule_id = `辖区.规范-版本.条号`；NDP 用参数覆盖层(同一 EC 规则切国别只换参数集)。

## 实施阶段（本项目内可落地的切片）
- **P1 规范引擎 MVP(进行中)**：`heng/codes/{dsl,rule,registry,jurisdiction}.py` + 受限DSL(ast白名单,非图灵完备) + 首批 CN-GB 规则(最小配筋率/轴压比/剪重比/位移角…)从硬编码转 Rule + 每条测试用例。辖区解析器(CN/JP/EU/US 生效规范集,确定性)。多国并行校核骨架 + EC NDP 参数层示例。
- **P2 SSM seed**：`heng/ssm.py` 语义模型 + Git式版本(commit/branch/tag/diff) + 审计日志。把 Project 投影进 SSM。
- **P3 玻璃盒**：CheckResult 带 provenance(rule_id+中间量)；规范RAG问答接口(检索条文库,附出处,无出处不作答)。
- **P4 交付**：计算书引用 rule_id 锚点；审查包(强条自查表)。
- 远期(设计书 Phase2/3)：水工模块、日本包产品化、迁移器、方案比选。属大工程，逐步。

## 进度
### P1 规范引擎 MVP —— ✅ 核心完成(2026-07-03)
新增 `heng/` 平台层：
- `heng/codes/dsl.py`：受限声明式 DSL（ast 白名单、非图灵完备、可静态审计）。算术/比较/布尔/三元/查表(interp/lookup)/白名单数学函数；拒绝 import/属性/下标/推导式/任意调用。
- `heng/codes/rule.py`：`Rule`(rule_id/scope/formula/provenance/lineage/test) + `CheckResult`(带溯源+全部中间量=玻璃盒) + `run_selftest`(规范库CI)。
- `heng/codes/rules_cn_gb.py`：**6 条中国 GB 规则**由硬编码重构而来（最小配筋率8.5.1、轴压比6.3.6、层间位移角5.5.1、周期比3.4.5、位移比3.4.3、剪重比5.2.5），每条附手算基准。
- `heng/codes/rules_eu_ec.py`：EC 规则（EN1992 最小配筋）演示 **NDP 参数覆盖层**。
- `heng/codes/jurisdiction.py`：**辖区解析器**(确定性) CN/EU/JP/US → 生效规范集/强条/审查流程；EC National Annex NDP。
- `heng/codes/registry.py`：注册表 + `check_all`/`scan`(全量条文扫描 + **强条红线**)。
- `heng/bridge.py`：把 analyze 结果投影为 ctx，用规范引擎复现整体指标判定(带溯源)。
- 测试(加入核心套件)：`test_heng_{dsl,rules,jurisdiction,bridge}.py` 共 20 项，全绿。含**规范库CI**(每条规则跑自带算例)、DSL安全、多国并行、NDP覆盖、**忠实性**(引擎复现硬编码判定)。

**关键成果**：验证了"把规范从代码剥离成数据"是**等价重构**（引擎判定==原硬编码），并带来玻璃盒溯源；且发现原硬编码剪重比用固定1.6%，数据规则按GB表5.2.5按烈度取值(8度0.032)**更精确**。

### P2 SSM seed —— ✅ 完成(2026-07-03)
`heng/ssm.py`：单一结构语义模型 + **Git 式版本化**：内容寻址 commit / branch(方案比选) / 带签名 tag(送审) / **diff(自动修改对照表)** / 全量审计日志；`ssm_from_project` 把现有 Project 投影为 SSM。测试 `test_heng_ssm.py` 6/6。

### 审查包 + 强条自查表 —— ✅ 完成(2026-07-03)
`heng/review.py`(设计书 §8⑧/§10/§15，直连北极星"强条漏检=0")：`review_package`(签名SSM快照+生效规范集+全量扫描+强条自查表) + `render_markdown`(送审审查包·强条自查表) + **强条红线**(不满足则禁止送审) + **签名门禁**(未签名=AI起草待确认,不得进送审包)。测试 `test_heng_review.py` 4/4。

### 规范问答(RAG-lite) —— ✅ 完成(2026-07-03)
`heng/qa.py`：条文级检索(中文2-gram，确定性)，答案必附 rule_id/条号/原文/是否强条；**无出处不作答**(禁止生成式补全)。测试 `test_heng_qa` 5/5。

### 构件级配筋校核入引擎 —— ✅ 完成(2026-07-03)
新增 5 条构件级 GB 规则(柱最小配筋率6.3.7/柱最大配筋率9.3.1/梁剪压比6.3.1/墙轴压比6.4.2/柱轴压比6.3.6)，
`heng/bridge.py::member_scan` 逐柱/墙/梁投影为 ctx 跑规则，**每条判定带条文出处**(玻璃盒到构件层)；
超限构件被标出且可溯源到具体条文。测试 `test_heng_member` 5/5。规则总数 11(CN10+EU1)，规范库 CI 全过。

### 双规范并行对照报告 —— ✅ 完成(2026-07-03)
`heng/dualcode.py`(§4.4 跨国杀手锏)：Rule 加 `concept` 概念标签；同一上下文两套规范各自独立校核、按概念配对，输出逐条对照(各自条文/判定/控制限值/差异说明)，**映射仅解释差异不换算**。示例：梁最小配筋率 中国8.5.1(0.20%) vs Eurocode 9.2.1.1(0.26fctm/fyk)。测试 `test_heng_dualcode` 4/4。

### 水工规范包 seed —— ✅ 完成(2026-07-03，设计书 §7 差异化主场)
`heng/codes/rules_cn_sl_nb.py`：把规范引擎从建筑扩到**水工**域(新增 gravity_dam/sluice 构件类型)——重力坝坝基抗滑(NB/T 35026 抗剪断公式)、水闸抗浮/抗滑(SL 265)，均强条+手算基准。辖区解析器按 structure_type 返回**水工规范集**(SL/NB)。测试 `test_heng_hydraulic` 5/5。**规范引擎现覆盖建筑+水工双域、14 条规则、2 辖区。**

### 模型迁移器 —— ✅ 完成(2026-07-03，设计书 §9)
`heng/migrate.py`：外部模型(PKPM/YJK/ETABS…导出通用格式)→SSM + **迁移诊断报告**(完整映射/语义有损/需人工补充/覆盖率)；不承诺100%无损、承诺100%透明；导入结果可进 SSM 版本库。测试 `test_heng_migrate` 5/5。

### 日本包 seed —— ✅ 完成(2026-07-03)
`heng/codes/rules_jp.py`：建築基準法施行令强制条文(令77条柱主筋≥0.8%、令82条の2层间变形角≤1/200)。**引擎现覆盖 CN/EU/JP 三辖区**。中日双规范对照示例：同一 0.003 层间位移角 → 中国1/550超限✗ vs 日本1/200满足✔。测试 `test_heng_jp` 5/5。

### P4 交付：规范引擎接入计算书 + GUI —— ✅ 完成(2026-07-03，玻璃盒端到端可见)
把 Code-as-Data 引擎从"内部对齐验证"推进到"**用户可见的交付物**"：
- `heng/calcsection.py::compliance_section`：渲染计算书「规范校核」章节——整体指标 + 构件级配筋校核 + 强条自查，
  **每条判定标注 rule_id / 条文号 / 条文原文 / 中间量**（"点击任何数字→依据链"的文本载体）。
- `modeler/run/analyze.py`：计算书自动追加该溯源章节（**加法 + 崩溃隔离** try/except，绝不拖垮既有计算书）。
- `modeler/app.py::_heng_review`：新增「🏛 规范审查」工具按钮 → 生成 **送审强条自查表 + 逐条溯源审查表**（含 SSM 签名快照、
  未签名=AI起草待确认门禁），写盘并打开。测试 `test_heng_calcsection` 2/2；ui_smoke 绿；GUI 端到端 headless 验证通过。

**至此 L3 规范引擎已打通到 L5 交付**：任一工程算完，计算书即带逐条条文锚点；送审审查包一键产出。

### 北极星辅助指标「强条漏检数=0」—— ✅ 可度量实现(2026-07-03，设计书 §15)
北极星「一次送审通过率」是市场结果不可一次编码达成；但其**辅助指标「强条漏检数(目标为零)」**
是可在代码里度量+回归守护的工程指标，本轮把它做成平台能力：
- `heng/redline.py::zero_miss_audit`：对引擎覆盖的**每一条强条**注入已知违例上下文(VIOLATION_SEEDS，逐条手算落错侧)，
  断言必被标红线(**零假阴性**)；再用每条自带合规算例验证**报警特异性**(防"永远报错"平凡刷零)；
  并强制**每条强条都有违例种子**(未覆盖按保守口径计入漏检)。指标 = 真漏检 + 未覆盖强条。
- 实测：**强条 11 条，漏检数 = 0，零漏检达标**。`test_heng_redline` 5/5（含全覆盖/特异性/逐条被标/渲染）。
- 已接入 GUI「🏛 规范审查」输出：审查表顶部报告"强条漏检数=N"。**新增强条须同时带合规算例+违例算例方能入库**，指标随规范库增长持续有效。

### 玻璃盒协议四条「AI 能力宪法」—— ✅ 可验证实现(2026-07-03，设计书 §6.1)
把玻璃盒从"承诺"变成"可回归断言"：`heng/glassbox.py::glassbox_audit`
- **可溯源**：16 条规则判定均挂 rule_id/条文号 + 计算中间量(values)。
- **可复核**：同输入同输出(确定性,无隐藏 AI 状态) + `decision_log` 完整记录输入/检索条文/中间量/判定/裁决者。
- **可编辑**：改任一输入，判定确定性翻转(下游重算)。
- **可关闭**：**静态审计规范校核路径(heng/codes/*)零 AI/网络依赖**——关掉 AI，平台退化为纯确定性计算软件仍完整可用。
- 实测四条全部成立；`test_heng_glassbox` 6/6。已接入 GUI「规范审查」输出。**AI 永不签字**：合规裁决走确定性引擎。

### 辖区解析器「100% 正确」—— ✅ 可验证守护(2026-07-03，设计书 §4.3 第一级)
辖区解析必须 100% 正确(故用规则表而非 AI)。`test_heng_jurisdiction_coverage` 把"正确"变断言：
- **确定性**(纯函数同输入同输出) · **覆盖矩阵**(CN/EU/JP/US 建筑 + CN 水工 各自规范集对照 §4.2 首发清单) ·
  **NDP 覆盖**(切国别只换参数) · **安全失败**(未知辖区抛 ValueError 绝不静默给错答案) ·
  **引用完整**(解析器 mandatory 引用的每个强条 rule_id 都真实存在于注册表，无悬空引用)。6/6。

### 内核可信度基准库 V&V —— ✅ 可发布实现(2026-07-03，设计书 §5.2)
"替换的前提是信任"。`heng/vnv.py::run_benchmarks` 对**自研 FEM/模态内核**跑经典闭合解基准
（悬臂 M=P·L、简支 wL²/8、固端 wL²/12、荷载守恒 V=P、SDOF T=2π√(m/k)、双层剪切型周期比 √((3+√5)/(3−√5))），
**用解析解验内核**（非重算公式）。实测 6 题全达标，**最大相对误差 1.82e-16（机器精度，远严于 §5.2 的 2% 商软对比阈）**。
公开报告 `docs/heng/内核基准报告.md`（每次发版全量重跑）；`test_heng_vnv` 4/4。同时是 §9 迁移工具验收标准。
（PKPM/YJK/ETABS 同模型对比需正版环境，属现场对标环节。）

### 单一结构数据模型贯穿全过程 —— ✅ 完整性守护(2026-07-03，设计书 §2.2 护城河②/§8)
`test_heng_workflow`：一个模型经 Project→SSM→分析→整体校核→构件校核→计算书章节→送审审查包
**全链路同源一致**——① SSM 无损投影楼层语义模型(不丢不增)；② 审查包构件数与 SSM 恒等；
③ 校核层与分析层 1:1；④ 审查包规范集与辖区解析同源；⑤ 计算书章节含整体扫描每条条文(交付层不另起炉灶)；
⑥ SSM 投影确定性(无重新建模漂移)。2/2。证明"单一语义模型贯穿方案到交付"这条护城河可回归。

### 生成式方案比选 —— ✅ 落地(2026-07-03，设计书 §6.2)
`heng/scheme.py`：围绕给定截面生成 N 个方案(每个 = 一个 **SSM 分支**)，各自跑真实分析内核，
输出多目标指标(用钢量/混凝土量=造价代理/层间位移角/周期比/规范是否满足)，标出 **Pareto 前沿**(非受支配)
与**推荐方案**(满足全部规范且用钢量最小)。实测：太小截面方案不可行(构件不满足规范)被排除，
中等方案支配大截面方案(两目标皆优)独占前沿。接入 GUI「⚖ 方案比选」按钮(围绕当前截面 0.7/1.0/1.3/1.6× 自动生成)。
`test_heng_scheme` 5/5。玻璃盒：每方案判定来自确定性分析+规范引擎，比选只排序，选型由工程师拍板。

### 控制条文标注 —— ✅ 落地(2026-07-03，设计书 §6.2「哪条规范卡住了它」)
`heng/governing.py::governing_clause`：对一个构件跑全部适用规则，用受限 DSL 直接对每条 verdict 两侧求值算
**紧迫度**(demand/capacity，=1 即在规范边界)，返回**控制条文**(最紧迫者)——即"卡住这个构件"的那条规范，
带 rule_id/条文号/两侧数值(玻璃盒可溯源)、对任意 rule_id 通用无需硬编码。已接入 `member_check`，
**计算书不满足构件表新增「控制条文(哪条卡住它)」列**(如 350×350 柱→9.3.1 最大配筋率利用率 867%)。`test_heng_governing` 5/5。

### 规范库扩充：正常使用极限状态(裂缝/挠度/锚固) —— ✅(2026-07-03)
`rules_cn_gb.py` 新增 3 条 GB 50010 serviceability 规则(挠度 3.4.3/裂缝宽度 3.4.5/锚固长度 8.3.1)，
**纯数据+手算算例，零引擎改动**——自动参与规范库 CI/玻璃盒/控制条文/问答。非强条(不入强条漏检)，
待分析层供数(span/w_max/la_provided)后自动进构件级校核。同时**加固 RAG-lite**：qa 增中文样板词停用集
(影响/计算/作用…)，守护"无出处不作答"(防 boilerplate 误命中)；`test_heng_qa` 增挠度/裂缝/锚固可检索 + 样板词不作答，7/7。

### 北美包 seed：US 升为"有条文库" —— ✅(2026-07-03，设计书 §4.2 北美包)
`heng/codes/rules_us.py`：首批 ACI 318-19 / ASCE 7-22 条文(梁最小配筋 §9.6.1.2、柱配筋率下限 §10.6.1.1、
容许层间位移角 §12.12.1)，US 从"辖区可解析"升为**有条文库**(4 辖区全部有规则)。**三方 story_drift 对照**：
同一无量纲 drift 下 CN(1/550=0.00182) < JP(0.005) < US(0.020) → 中国最严，dualcode 正确判"CN 更严控制"。
`test_heng_us` 4/4。纯数据入库、零引擎改动，自动参与 CI/玻璃盒/问答/双规范对照。

**当前规范引擎：22 条规则(11 强条) · 4 辖区均有条文库(CN/EU/JP/US) · 2 域(建筑/水工) · 每条附算例(规范库CI全过) · 强条漏检数=0 · 玻璃盒四条成立 · 辖区解析确定性+100%覆盖 · 内核基准6题机器精度达标 · 单一模型贯穿全过程 · 生成式方案比选(Pareto) · 控制条文标注 · 承载力+正常使用双极限状态 · CN/JP/US 三方对照。**
**当前套件：modeler 25 文件 + core+heng 52 文件(原30+钢结构+heng 21文件~100测试) 全绿；全部已开源同步 GitHub/Gitee。**

## 已实现 vs 多年产品(诚实边界)
**已在本仓库落地并测试(可实现在代码里的核心差异化)**：L3规范引擎(Code-as-Data 6子系统) · L1 SSM版本化 · 玻璃盒/审查包/强条自查表 · 规范RAG问答 · 双规范并行(§4.4) · 水工域(§7) · 迁移器(§9) · CN/EU/JP多国。
**仍属多年产品+市场工程(非一次编码可"实现")**：各辖区**全量条文库**(须持证规范工程师逐条编DSL+审核,§4.3三级流水线) · 生成式方案比选(LLM编排+云端并行) · IFC-BIM移交 · 数字孪生反演 · 非线性/时程/耦合内核深化 · **大臣認定/软件测评(认证)** · **生产环境一次送审通过率验证**(北极星,须实际设计院/审图机构)。北极星是市场指标,本阶段实现其**机制抓手**(强条漏检=0自查表+红线门禁),证明架构可行。

### 下一步（roadmap 对齐，均为设计书 Phase2/3 的大工程）
- ~~把规范引擎接入计算书/GUI(每个数字→依据链)~~ ✅ 已完成(见 P4 交付)。后续把 `checks_table` 本身也切换为 `heng_scan` 直接产出(目前是并行追加溯源章节，双写→单一数据源)。
- 扩 GB55 系列全文强制条文 + 更多构件级(裂缝/挠度/锚固)。
- 水工规范包(SL 191/744、NB 35047…) + 水工工况组合引擎；日本包(告示体系)产品化；欧美包。
- 迁移器(PKPM/YJK/ETABS 导入 + 迁移诊断报告)；双规范并行逐构件对照报告；生成式方案比选；IFC-BIM 移交。
> 这些是多年、跨子系统的产品级工程；本仓库已落地并验证**核心差异化骨架**(L3规范引擎+L1 SSM+玻璃盒/审查+构件级)，与既有 L2 内核/L5 出图打通。北极星"送审通过率"是生产环境市场指标，本阶段实现其**机制抓手**(强条漏检=0 自查表+红线门禁)。
